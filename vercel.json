{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "sh scripts/prisma-env.sh migrate deploy && sh scripts/prisma-env.sh generate && next build",
  "framework": "nextjs",
  "ignoreCommand": "bash -c 'if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then exit 1; else exit 0; fi'",
  "regions": ["lhr1"]
}
